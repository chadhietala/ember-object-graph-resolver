var controller = 'export default Ember.Controller.extend({' +
                    'needs: "post",' +
                    'bar: "foo",' +
                    'f: function() {}' +
                  '});';

module.exports = controller;